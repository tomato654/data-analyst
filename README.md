# Run backend

1. `cd ./server`

1. Install all of python dependencies

   `conda create --name <env> --file requirements.txt`

2. run app.py `python app.py`

# Run Frontend

1. Install all of dependencies

   `npm i`

2. run localhost

   `npm run start`

# Database

Install mongoDB on Local machine

